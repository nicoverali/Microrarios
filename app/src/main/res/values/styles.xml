<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:actionMenuTextColor">@color/white</item>
    </style>

    <style name="SplashScreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/ic_splash_screen_background</item>
    </style>
    
    <!--Spinners styles-->
    <style name="SpinnerItemStyle" parent="android:Widget.TextView.SpinnerItem">
    </style>
    <style name="SpinnerDropdownItemStyle" parent="android:TextAppearance.Widget.TextView.SpinnerItem">
        <item name="android:background">@drawable/ic_top_bottom_bordered</item>
    </style>

    <!--Button style-->
    <style name="ButtonLoadMoreStyle" parent="@android:style/Widget.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/text_color_state</item>
    </style>
</resources>
